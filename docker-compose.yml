version: '3.8'

services:
  bot:
    build: .
    env_file: .env
    environment:
      - DB_PATH=/app/data/database.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    stop_grace_period: 10s
